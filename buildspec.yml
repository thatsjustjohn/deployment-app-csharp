version: 0.2

phases:

  build:
    commands:
      - dotnet build
      - dotnet publish
artifacts:
  files:
    - ./basic-web-app/site/**/*
    - deployment-app-csharp/basic-web-app/aws-windows-deployment-manifest.json
